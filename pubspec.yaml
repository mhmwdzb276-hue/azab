name: azab_x
description: A custom video editor with full Arabic dialect support.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
audioplayers: ^6.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter_lints

flutter:
  uses-material-design: true